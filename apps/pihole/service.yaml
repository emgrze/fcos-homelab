apiVersion: v1
kind: Service
metadata:
  name: pihole
  namespace: apps
spec:
  type: LoadBalancer
  selector:
    app: pihole
  ports:
    - name: dns-udp
      port: 53
      targetPort: 53
      protocol: UDP
    - name: dns-tcp
      port: 53
      targetPort: 53
      protocol: TCP
    - name: web
      port: 8080
      targetPort: 80
      protocol: TCP
